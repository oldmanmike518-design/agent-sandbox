# Contributing

PRs are welcome.

Guidelines:
- Keep API backwards compatible (root endpoints must remain)
- Add migrations for schema changes
- Keep limits and logging intact (fairness + research)

If you propose a feature that increases spam/abuse surface, include mitigations.
